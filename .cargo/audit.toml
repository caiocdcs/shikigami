# cargo-audit configuration. See https://github.com/rustsec/rustsec/tree/main/cargo-audit

[advisories]
ignore = [
    # proc-macro-error2 unmaintained. Build-time proc-macro pulled in
    # transitively by validator_derive (validator 0.20, latest). Not in the
    # runtime binary, no upstream fix available yet. Drop this once validator
    # migrates off proc-macro-error2.
    "RUSTSEC-2026-0173",
]
